<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Complete User Profile</title>
    <style>
        :root {
            --primary: #1a365d;
            --secondary: #2b6cb0;
            --accent: #63b3ed;
            --background: #ebf8ff;
            --text: #1a202c;
            --success: #48bb78;
            --white: #ffffff;
            --error: #dc2626;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--background);
            color: var(--text);
            font-family: Arial, sans-serif;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 2rem;
        }

        .card {
            background-color: var(--white);
            width: 100%;
            max-width: 800px;
            padding: 3rem;
            border-radius: 16px;
            box-shadow: 0 0 30px rgba(26, 54, 93, 0.15);
        }

        .card-header {
            text-align: center;
            color: var(--primary);
            margin-bottom: 3rem;
        }

        .card-header h2 {
            font-size: 2.5rem;
            font-weight: bold;
            margin: 0;
        }

        .form-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 2rem;
        }

        @media (max-width: 768px) {
            .form-grid {
                grid-template-columns: 1fr;
            }
        }

        .form-group {
            margin-bottom: 2rem;
        }

        .label {
            color: var(--primary);
            font-weight: bold;
            display: block;
            margin-bottom: 1rem;
            font-size: 1.1rem;
        }

        .input, select {
            background-color: var(--white);
            color: var(--text);
            border: 2px solid var(--secondary);
            padding: 1rem;
            border-radius: 8px;
            width: 100%;
            font-size: 1.1rem;
            transition: all 0.3s ease;
        }

        .input:focus, select:focus {
            outline: none;
            border-color: var(--accent);
            box-shadow: 0 0 15px rgba(99, 179, 237, 0.2);
        }

        select {
            cursor: pointer;
        }

        .radio-group {
            display: flex;
            gap: 3rem;
            margin-top: 0.8rem;
        }

        .radio-group div {
            display: flex;
            align-items: center;
            gap: 0.8rem;
        }

        .radio-group input[type="radio"] {
            accent-color: var(--secondary);
            width: 1.4rem;
            height: 1.4rem;
            cursor: pointer;
        }

        .radio-group label {
            cursor: pointer;
            color: var(--text);
            font-size: 1.1rem;
        }

        .slider {
            width: 100%;
            height: 6px;
            background: var(--background);
            border-radius: 6px;
            appearance: none;
            margin-top: 1.5rem;
        }

        .slider::-webkit-slider-thumb {
            appearance: none;
            width: 24px;
            height: 24px;
            background: var(--secondary);
            border-radius: 50%;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .slider::-webkit-slider-thumb:hover {
            background: var(--accent);
        }

        .button {
            background-color: var(--primary);
            color: var(--white);
            padding: 1.2rem;
            border: none;
            width: 100%;
            border-radius: 8px;
            cursor: pointer;
            font-size: 1.2rem;
            font-weight: bold;
            transition: all 0.3s ease;
            margin-top: 2rem;
        }

        .button:hover {
            background-color: var(--secondary);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(43, 108, 176, 0.3);
        }

        .button:active {
            transform: translateY(0);
        }

        .value-display {
            color: var(--secondary);
            font-weight: bold;
            font-size: 1.1rem;
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="card-header">
            <h2>Complete Your Profile</h2>
        </div>
        <form action="/about" method="POST">
            <div class="form-grid">
                <div class="form-group">
                    <label class="label" for="name">Name</label>
                    <input type="text" class="input" id="name" name="name" required>
                </div>

                <div class="form-group">
                    <label class="label" for="age">Age</label>
                    <input type="number" class="input" id="age" name="age" required>
                </div>

                <div class="form-group">
                    <label class="label" for="gender">Gender</label>
                    <select id="gender" name="gender" class="input" required>
                        <option value="" disabled selected hidden>Select gender</option>
                        <option value="male">Male</option>
                        <option value="female">Female</option>
                        <option value="other">Other</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="label" for="profession">Profession</label>
                    <input type="text" class="input" id="profession" name="profession" placeholder="Enter your profession" required>
                </div>

                <div class="form-group">
                    <label class="label" for="height">Height (in meters)</label>
                    <input type="number" class="input" id="height" name="height" step="0.01" required>
                </div>

                <div class="form-group">
                    <label class="label" for="weight">Weight (in kg)</label>
                    <input type="number" class="input" id="weight" name="weight" step="0.1" required>
                </div>

                <div class="form-group">
                    <label class="label" for="workoutExperience">Previous Workout Experience</label>
                    <select id="workoutExperience" name="Prev_Workout_Exp" class="input" required>
                        <option value="" disabled selected hidden>Select experience level</option>
                        <option value="beginner">Beginner</option>
                        <option value="intermediate">Intermediate</option>
                        <option value="expert">Expert</option>
                    </select>
                </div>

                <div class="form-group">
                    <label class="label" for="pain-area">Pain Area</label>
                    <div class="radio-group">
                        <div>
                            <input type="radio" id="neck" name="pain_area" value="neck">
                            <label for="neck">Neck</label>
                        </div>
                        <div>
                            <input type="radio" id="back" name="pain_area" value="back">
                            <label for="back">Back</label>
                        </div>
                    </div>
                </div>
            </div>

            <div class="form-group">
                <label class="label" for="activity_time">Daily Activity Time: <span id="activity_time_value" class="value-display">30</span> minutes</label>
                <input type="range" class="slider" id="activity_time" name="activity_time" min="0" max="120" value="30" step="5">
            </div>

            <button type="submit" class="button">Create Profile</button>
        </form>
    </div>

    <script>
        const slider = document.getElementById('activity_time');
        const output = document.getElementById('activity_time_value');
        
        function updateSlider() {
            output.textContent = slider.value;
        }

        slider.addEventListener('input', updateSlider);
        slider.addEventListener('change', updateSlider);
        
        updateSlider();
    </script>
</body>
</html>
