<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OTP Verification</title>
    <style>
        :root {
            --primary: #1a365d;
            --secondary: #2b6cb0;
            --accent: #63b3ed;
            --background: #ebf8ff;
            --text: #1a202c;
            --success: #48bb78;
            --white: #ffffff;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: Arial, sans-serif;
            background-color: var(--background);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            color: var(--text);
        }

        .container {
            background-color: var(--white);
            padding: 2rem;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            width: 90%;
            max-width: 400px;
        }

        h1 {
            color: var(--primary);
            text-align: center;
            margin-bottom: 1.5rem;
            font-size: 1.5rem;
        }

        p {
            text-align: center;
            margin-bottom: 1.5rem;
            color: var(--secondary);
        }

        .otp-container {
            display: flex;
            justify-content: center;
            margin-bottom: 1.5rem;
        }

        .otp-input {
            width: 100%;
            height: 45px;
            font-size: 1.5rem;
            text-align: center;
            border: 2px solid var(--accent);
            border-radius: 8px;
            background-color: var(--white);
            color: var(--text);
            transition: all 0.3s ease;
            padding: 0.5rem;
        }

        .otp-input:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 2px rgba(26, 54, 93, 0.2);
        }

        .submit-btn, .generate-btn {
            width: 48%;
            padding: 0.8rem;
            background-color: var(--primary);
            color: var(--white);
            border: none;
            border-radius: 8px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .submit-btn:hover, .generate-btn:hover {
            background-color: var(--secondary);
        }

        .btn-container {
            display: flex;
            justify-content: space-between;
            gap: 1rem;
        }

        .resend {
            text-align: center;
            margin-top: 1rem;
        }

        .resend a {
            color: var(--secondary);
            text-decoration: none;
            font-size: 0.9rem;
        }

        .resend a:hover {
            color: var(--primary);
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>OTP Verification</h1>
        <p>Please enter the verification code sent to your device</p>
        
        <form onsubmit="return validateForm(event)" action="\generate_otp" ,method="post">
            <div class="otp-container">
                <input type="text" class="otp-input" maxlength="6" pattern="[0-9]{6}" required>
            </div>
            <div class="btn-container">
                <button type="button" class="generate-btn" onclick="generateOtp()">Generate OTP</button>
                <button type="submit" class="submit-btn">Submit OTP</button>
            </div>
        </form>

        
    </div>

    <script>
        // Form validation
        function validateForm(event) {
            event.preventDefault();
            const otp = document.querySelector('.otp-input').value;
            if (otp.length === 6) {
                alert('OTP Submitted: ' + otp);
                // Here you would typically make an API call to verify the OTP
            } else {
                alert('Please enter a valid 6-digit OTP');
            }
            return false;
        }
    </script>
</body>
</html>
